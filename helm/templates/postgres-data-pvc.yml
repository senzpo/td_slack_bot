---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data-pvc-{{ .Values.serviceName }}
  namespace: {{ .Values.namespace }}
  labels:
    app: postgres-{{ .Values.serviceName }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
